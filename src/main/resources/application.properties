spring.application.name=BankSystem

server.port=8080

# JPA
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# Logging
logging.level.root=INFO
logging.level.com.example.BankSystem=INFO

# Do not put database credentials or JWT secrets here.
# Profile is activated externally.